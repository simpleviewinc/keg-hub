version: "2"
services:
  core:
    volumes:
      - core-sync:/keg/tap/node_modules/keg-core:nocopy
      - cli-sync:/keg/cli:nocopy
volumes:
  core-sync:
    external:
      name: "core-unison-sync"
  cli-sync:
    external:
      name: "cli-unison-sync"