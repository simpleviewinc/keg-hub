version: "3.8"
services:
  kegcore:
    volumes:
      - core-sync:${DOC_APP_PATH}:nocopy
      - cli-sync:/keg/keg-cli:nocopy
volumes:
  core-sync:
    external:
      name: "core-unison-sync"
  cli-sync:
    external:
      name: "cli-unison-sync"