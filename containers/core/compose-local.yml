version: "2"
services:
  kegcore:
    volumes:
      - core-sync:/keg/keg-core:nocopy
      - cli-sync:/keg/keg-cli:nocopy
volumes:
  core-sync:
    external:
      name: "core-unison-sync"
  cli-sync:
    external:
      name: "cli-unison-sync"