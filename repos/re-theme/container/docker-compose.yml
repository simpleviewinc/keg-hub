version: "3.8"
services:
  retheme:
    environment:
      - DOC_APP_PATH
      - DOC_BUILD_PATH
      - DOC_JSUTILS_PATH
      - KEG_PROXY_PORT
      - ENV
      - KEG_EXEC_CMD
      - KEG_DOCKER_EXEC
      - KEG_NM_INSTALL
      - NODE_ENV

