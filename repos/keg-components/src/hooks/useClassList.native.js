import { noPropArr } from '../utils/helpers/noop'

/**
 * Placeholder hook for Native platforms
 * <br/>Just returns an empty array
 *
 * @returns {Array} - Empty array
 */
export const useClassList = () => {
  return noPropArr
}
