<!--- Touchable.stories.mdx -->

import { Story, Meta, Preview, Props, Description } from '@storybook/addon-docs/blocks'

import { Touchable } from './touchable'
import {Touchable as TouchableNative} from './touchable.native.js'
import {Basic, WithStates} from './touchable.examples.js'

<Meta title="Components/Interactions/Touchable" component={Touchable}/>

# Touchable

Base component for handling touch interactions. 
Utilizes react-native [Pressable](https://reactnative.dev/docs/pressable) under the hood

## Examples

### Basic
<Preview>
  <Story name="Basic">
    <Basic />
  </Story>
</Preview>

### With States
You can have control over the states by passing in `children` as a function.
It gives you access to these 3 states: **pressed**(`web, native`), **hovered**(`web`), **focused**(`web`). The value reflects the current state of the component
```js
<Touchable
  children={({ pressed, hovered }) => {}
/>
```
<Preview>
  <Story name='With States'>
    <WithStates/>
  </Story>
</Preview>

## Props
Supports all props on [Pressable](https://reactnative.dev/docs/pressable#props) 
along with: 

<Props of={TouchableNative} />

