import { colors } from '../colors'

export const divider = {
  $all: {
    width: "100%",
    backgroundColor: colors.content.divider,
    marginBottom: 15,
    marginTop: 5,
    height: 1
  },
  $native: {
    hairlineWidth: 1,
  }
}