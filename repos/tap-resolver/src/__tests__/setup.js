const { FS } = require('../../mocks')
jest.setMock('fs', FS)

const Setup = require('../setup')

describe('Setup', () => {
  
  beforeEach(() => {
    
  })
  
  it('', () => {
    expect(true).toBe(true)
  })

})
