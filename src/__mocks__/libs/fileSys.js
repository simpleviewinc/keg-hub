const copyStream = jest.fn(() => {
  
})
const getFiles = jest.fn(() => {
  
})
const getFilesSync = jest.fn(() => {
  
})
const getFolders = jest.fn(() => {
  
})
const getFoldersSync = jest.fn(() => {
  
})
const getFolderContent = jest.fn(() => {
  
})
const mkDir = jest.fn(() => {
  
})
const movePath = jest.fn(() => {
  
})
const pathExists = jest.fn(() => {
  
})
const pathExistsSync = jest.fn(() => {
  
})
const readFile = jest.fn(() => {
  
})
const readFileSync = jest.fn(() => {
  
})
const writeFile = jest.fn(() => {
  
})
const writeFileSync = jest.fn(() => {
  
})

const loadENV = jest.fn(() => {
  
})

const loadYml = jest.fn(() => {
  
})

const writeYml = jest.fn(() => {
  
})

module.exports = {
  copyStream,
  getFiles,
  getFilesSync,
  getFolders,
  getFoldersSync,
  getFolderContent,
  loadENV,
  loadYml,
  mkDir,
  movePath,
  pathExists,
  pathExistsSync,
  readFile,
  readFileSync,
  writeFile,
  writeFileSync,
  writeYml,
}